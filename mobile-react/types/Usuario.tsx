export type Usuario = {
  id: string;
  name: string;
  email: string;
  password: string;
  role: string;
};

export type UsuarioForm = {
  nome: string;
  email: string;
  senha: string;
  role: string;
};
